# Kent Test 01

A FastAPI project initialized with opencode.

## Quick Start

```bash
# Install dependencies
pip install -e .

# Run the server
uvicorn app.main:app --reload
```

Visit `http://localhost:8000/docs` for the interactive API docs.
